---
const steps = [
  {
    title: 'Descrie lucrarea',
    description: 'Spune-ne de ce ai nevoie în mai puțin de 60 de secunde.',
    step: '01',
  },
  {
    title: 'Primește oferte',
    description: 'Compară oferte clare de la meseriași verificați din zona ta.',
    step: '02',
  },
  {
    title: 'Alege în siguranță',
    description: 'Selectează cea mai bună ofertă și pornește lucrarea fără bătăi de cap.',
    step: '03',
  },
];
---

<section class="w-full bg-white" id="cum-functioneaza">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-14 sm:py-16 lg:py-20">
    <div class="mb-10 sm:mb-12 text-center">
      <h2 class="text-3xl sm:text-4xl font-extrabold tracking-tight text-slate-900">
        Proiectul tău, rezolvat în 3 pași simpli
      </h2>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-3 gap-6 sm:gap-8 lg:gap-10">
      {steps.map((step) => (
        <div class="relative group">
          <div class="relative h-full rounded-2xl bg-white shadow-[0_10px_30px_rgba(0,0,0,0.08)] ring-1 ring-black/5 transition-all duration-300 hover:shadow-[0_16px_40px_rgba(0,0,0,0.12)] hover:-translate-y-0.5">
            <div class="relative flex flex-col items-center text-center px-6 py-8 sm:px-8 sm:py-10">
              <div class="relative mb-6">
                <div class="relative w-16 h-16 sm:w-20 sm:h-20 rounded-full bg-gradient-to-br from-amber-500 to-rose-500 text-white flex items-center justify-center shadow-lg ring-4 ring-amber-500/20">
                  <span class="text-xl sm:text-2xl font-extrabold">{step.step}</span>
                </div>
              </div>
              <h3 class="text-xl sm:text-2xl font-extrabold mb-1 text-slate-900">
                {step.title}
              </h3>
              <p class="text-slate-600 max-w-sm text-sm sm:text-base">
                {step.description}
              </p>
            </div>
          </div>
        </div>
      ))}
    </div>

    <div class="text-center mt-12 sm:mt-16">
      <a
        href="https://app.meseriaslocal.ro/cere-oferta"
        class="inline-flex items-center gap-3 px-7 sm:px-9 py-3.5 sm:py-4 rounded-full bg-gradient-to-r from-orange-500 via-amber-500 to-rose-500 text-white font-semibold transition-all duration-300 hover:brightness-110 shadow-lg"
      >
        <span class="relative">Postează o lucrare gratuit</span>
        <svg class="relative w-4 h-4 sm:w-5 sm:h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6" />
        </svg>
      </a>
    </div>
  </div>
</section>
