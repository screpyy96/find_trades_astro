---
import Base from '../layouts/Base.astro';
import { trades } from '../data';

// Popular trades pentru quick links
const popularTrades = trades.slice(0, 6);

// Popular cities
const popularCities = [
  { name: 'București', slug: 'bucuresti' },
  { name: 'Cluj-Napoca', slug: 'cluj-napoca' },
  { name: 'Timișoara', slug: 'timisoara' },
  { name: 'Iași', slug: 'iasi' },
  { name: 'Constanța', slug: 'constanta' },
  { name: 'Brașov', slug: 'brasov' },
];
---

<Base 
  title="Pagină negăsită (404) - Meserias Local"
  description="Pagina pe care o cauți nu există. Descoperă meseriași profesioniști sau postează o cerere de ofertă gratuită."
>
  <div class="min-h-screen bg-gradient-to-br from-orange-50 via-white to-amber-50 flex items-center justify-center px-4 py-16">
    <div class="max-w-6xl w-full">
      
      <!-- Hero Section -->
      <div class="text-center mb-12">
        <!-- 404 Illustration -->
        <div class="mb-8 relative">
          <div class="text-[180px] md:text-[240px] font-black text-transparent bg-clip-text bg-gradient-to-r from-orange-400 to-amber-500 leading-none select-none">
            404
          </div>
          <div class="absolute inset-0 flex items-center justify-center">
            <div class="text-6xl animate-bounce">🔧</div>
          </div>
        </div>

        <!-- Title & Description -->
        <h1 class="text-3xl md:text-5xl font-bold text-slate-900 mb-4">
          Ups! Pagina nu există
        </h1>
        <p class="text-lg md:text-xl text-slate-600 max-w-2xl mx-auto mb-8">
          Dar nu-i bai! Avem ceva mai bun pentru tine. Găsește meseriașul potrivit sau postează o cerere gratuită.
        </p>

        <!-- Primary CTAs -->
        <div class="flex flex-col sm:flex-row gap-4 justify-center mb-12">
          <a 
            href="https://app.meseriaslocal.ro/cere-oferta"
            class="group relative inline-flex items-center justify-center gap-2 px-8 py-4 bg-gradient-to-r from-orange-500 to-amber-500 text-white font-bold rounded-xl shadow-xl hover:shadow-2xl hover:scale-105 transition-all duration-200"
          >
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
            </svg>
            <span>Cere Ofertă Gratuită</span>
            <span class="absolute -top-2 -right-2 bg-green-500 text-white text-xs font-bold px-2 py-1 rounded-full animate-pulse">
              24h
            </span>
          </a>
          
          <a 
            href="/meseriasi"
            class="inline-flex items-center justify-center gap-2 px-8 py-4 bg-white text-slate-900 font-semibold rounded-xl border-2 border-slate-300 hover:border-orange-500 hover:bg-orange-50 transition-all duration-200"
          >
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
            </svg>
            <span>Caută Meseriași</span>
          </a>
        </div>
      </div>

      <!-- Popular Services Grid -->
      <div class="mb-12">
        <h2 class="text-2xl font-bold text-slate-900 text-center mb-6">
          🔥 Servicii Populare
        </h2>
        <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-4">
          {popularTrades.map((trade) => (
            <a
              href={`/servicii/${trade.category.toLowerCase().replace(/\s+/g, '-')}/${trade.slug}`}
              class="group bg-white rounded-xl p-4 border-2 border-slate-200 hover:border-orange-500 hover:shadow-lg transition-all duration-200 text-center"
            >
              <div class="text-3xl mb-2 group-hover:scale-110 transition-transform">
                {trade.icon}
              </div>
              <div class="text-sm font-semibold text-slate-900 group-hover:text-orange-600">
                {trade.name}
              </div>
            </a>
          ))}
        </div>
      </div>

      <!-- Popular Cities -->
      <div class="mb-12">
        <h2 class="text-2xl font-bold text-slate-900 text-center mb-6">
          📍 Orașe Populare
        </h2>
        <div class="flex flex-wrap justify-center gap-3">
          {popularCities.map((city) => (
            <a
              href={`/meseriasi?city=${city.slug}`}
              class="px-6 py-3 bg-white rounded-full border-2 border-slate-200 hover:border-orange-500 hover:bg-orange-50 font-semibold text-slate-900 hover:text-orange-600 transition-all duration-200"
            >
              {city.name}
            </a>
          ))}
        </div>
      </div>

      <!-- Help Section -->
      <div class="bg-white rounded-2xl shadow-xl p-8 border-2 border-slate-200">
        <div class="grid md:grid-cols-3 gap-8">
          
          <!-- Contact Direct -->
          <div class="text-center">
            <div class="w-16 h-16 bg-gradient-to-br from-blue-500 to-blue-600 rounded-2xl flex items-center justify-center mx-auto mb-4">
              <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"></path>
              </svg>
            </div>
            <h3 class="text-lg font-bold text-slate-900 mb-2">Sună Meseriașii</h3>
            <p class="text-slate-600 text-sm mb-4">
              Contactează direct profesioniștii verificați din zona ta
            </p>
            <a href="/meseriasi" class="text-blue-600 hover:text-blue-700 font-semibold text-sm">
              Vezi meseriași →
            </a>
          </div>

          <!-- Post Request -->
          <div class="text-center">
            <div class="w-16 h-16 bg-gradient-to-br from-orange-500 to-amber-500 rounded-2xl flex items-center justify-center mx-auto mb-4">
              <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
              </svg>
            </div>
            <h3 class="text-lg font-bold text-slate-900 mb-2">Postează Cerere</h3>
            <p class="text-slate-600 text-sm mb-4">
              Primești oferte de la meseriași în 24h, complet gratuit
            </p>
            <a href="https://app.meseriaslocal.ro/cere-oferta" class="text-orange-600 hover:text-orange-700 font-semibold text-sm">
              Cere ofertă →
            </a>
          </div>

          <!-- AI Assistant -->
          <div class="text-center">
            <div class="w-16 h-16 bg-gradient-to-br from-purple-500 to-purple-600 rounded-2xl flex items-center justify-center mx-auto mb-4">
              <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 10h.01M12 10h.01M16 10h.01M9 16H5a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v8a2 2 0 01-2 2h-5l-5 5v-5z"></path>
              </svg>
            </div>
            <h3 class="text-lg font-bold text-slate-900 mb-2">Întreabă AI-ul</h3>
            <p class="text-slate-600 text-sm mb-4">
              Asistentul nostru AI te ajută să găsești rapid ce cauți
            </p>
            <a href="https://app.meseriaslocal.ro/ajutor" class="text-purple-600 hover:text-purple-700 font-semibold text-sm">
              Deschide chat →
            </a>
          </div>

        </div>
      </div>

      <!-- Back to Home -->
      <div class="text-center mt-12">
        <a 
          href="/"
          class="inline-flex items-center gap-2 text-slate-600 hover:text-orange-600 font-semibold transition-colors"
        >
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
          </svg>
          <span>Înapoi la pagina principală</span>
        </a>
      </div>

    </div>
  </div>

  <!-- Analytics Tracking -->
  <script is:inline>
    // Track 404 page view
    if (window.dataLayer) {
      window.dataLayer.push({
        event: '404_page_view',
        page_location: window.location.href,
        page_referrer: document.referrer
      });
    }

    // Track CTA clicks
    document.addEventListener('DOMContentLoaded', () => {
      document.querySelectorAll('a[href*="cere-oferta"]').forEach(btn => {
        btn.addEventListener('click', () => {
          if (window.dataLayer) {
            window.dataLayer.push({
              event: 'cere_oferta_click',
              category: 'conversion',
              action: 'click',
              label: '404_page_cta',
              page_location: window.location.href
            });
          }
        });
      });
    });
  </script>
</Base>
